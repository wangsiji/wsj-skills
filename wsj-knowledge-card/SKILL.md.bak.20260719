---
name: knowledge-card
description: 将互联网知识，通过 AI 蒸馏成结构化知识，再通过人的实践和反思，转化成个人资产。AI 提炼公共知识，人沉淀私人知识。
triggers:
  - 知识卡片
  - 知识卡
  - 整理知识
  - 做一张卡片
  - 整理成卡片
  - 常青卡片
  - 帮我整理
  - 这个概念帮我写成卡片
  - 写一张知识卡
---

# Knowledge Card · 个人知识引擎

> **把互联网知识，通过 AI 蒸馏成结构化知识，再通过人的实践和反思，转化成个人资产。**
>
> **AI 提炼公共知识，人沉淀私人知识。**

---

## 核心定位

把外部信息（书籍/剪藏/想法/概念）经过 **问题化 → 结构化 → 关联化 → 个人化** 四步加工，转化为一张可长期积累、可被 AI 理解、可双向链接的 Evergreen 卡片。

```
信息输入（书籍/文章/视频/聊天/想法）
    ↓
clip — 收集原始素材 → Raw
    ↓
knowledge-card — 知识蒸馏
    Problem → Concept → Relation → Example → Action → Boundary
    ↓
Human — 个人内化
    Insight（我的理解 / 我的经历 / 我的迁移）
    ↓
Evergreen — 长期知识资产
    ↓
AI Retrieval — 未来 AI 基于个人知识库回答
```

---

## 输出格式

### Frontmatter

**简化版（推荐，够用）：**

```yaml
---
name: 核心词
description: 一句话结论（完整立场句）
category:
  - "[[33-学习]]"
tags: [常青卡片]
source:
  - 《书名》
  - URL
status: learning
---
```

| 字段 | 说明 |
|------|------|
| `source` | 知识来源（书/链接/作者） |
| `status` | 成熟度：`seed` → `learning` → `practiced` → `evergreen` |

**可选扩展字段（仅当用户明确要求时才启用；默认不用）：**

```yaml
source_type: [book, research]
confidence:
  level: medium
  reason: 有实验研究支持
```

| 可选字段 | 说明 |
|---------|------|
| `source_type` | 来源类型：`book` / `research` / `article` / `experience` |
| `confidence` | 可信度，含 `level`（high/medium/low）和 `reason`

---

### 正文格式（7 节，6 节 AI 生成 + 1 节用户内化）

> ⚠️ **节间规则**：不用 `---` 分割线 — callout + 标题已足够分隔。不用居中标签。卡片文件名 = 核心词，不带 `# ` 标题行。

```markdown
---
name: 核心词
description: 一句话结论
category:
  - "[[33-学习]]"
tags: [常青卡片]
source: []
status: learning
---

## 1. 问题（Problem）

> [!warning] 信念冲突标题
> | 你以为 | 实际发生 |
> |---|---|
> | 常见错误认知 1 | 真实的结论 1 |
> | 常见错误认知 2 | 真实的结论 2 |

## 2. 概念（Concept）

### 📖 定义
一句话定义。**核心观点加粗。**

### 🔍 本质
底层机制+类比。为什么有效。

### 🔬 来源与依据
| 提出者/理论 | 依据内容 |
|---|---|
| 人名/来源 | 摘要 |

### 🚫 常见误解
| ❌ 误解 | ✅ 实际 |
|---|---|
| 误解 | 澄清 |

## 3. 关系（Relation）

| 类型 | 相关概念 | 关系说明 |
|---|---|---|
| **上位** | [[概念]] `说明` | 关系 |
| **相似** | [[概念]] `说明` | 关系 |
| **对立** | [[概念]] `说明` | 关系 |
| **应用** | [[概念]] `说明` | 关系 |

## 4. 案例（Example）

> [!example] 故事标题
> 案例内容
>
> > [!tip] 小提示
> > 一句话

## 5. 行动（Action）

> [!check] 标题
> | 步骤 | 动作 | 关键 |
> |---|---|---|
> | 1 | 第一步 | 要点 |

## 6. 边界（Boundary）

| ✅ 适用 | ❌ 不适用 |
|---|---|
| 场景 1 | 场景 1 |

## 7. 内化（Insight）

> [!abstract] ⚠️ 此部分由你填写
> | 维度 | 你的回答 |
> |---|---|
> | 我的理解 | |
> | 我的经历 | |
> | 我的迁移 | |
```

#### 各节 callout 映射速查

| 节 | Callout | 说明 |
|----|---------|------|
| Problem | `[!warning]` | 「你以为 vs 实际」信念冲突表 |
| Concept | 纯标题 `📖🔍🔬🚫` | 定义/本质/来源/误解，每个子节一张表格 |
| Relation | 纯表格 | 4 类（上位/相似/对立/应用），`[[wikilink]]` |
| Example | `[!example]` | 可嵌套 `[!tip]` |
| Action | `[!check]` | 3列表（步骤/动作/关键） |
| Boundary | 纯表格 | `✅ 适用 \| ❌ 不适用` |
| Insight | `[!abstract]` | 留空的 3 行表（理解/经历/迁移） |

---

## 各节写作指南

### 1. 问题（Problem）
- **开门见一个痛点场景**：读者在什么情况下会需要这个知识？
- 用「你是否遇到过……」「为什么我明明……却……」
- ✅ 「为什么看懂了书，考试不会做题？」
- ❌ 「这个概念很重要」

---

### 2. 概念（Concept）

回答三个问题：**它是什么？为什么相信它？它不是什么？**

#### 定义（Definition）
`X 是一种通过 A 达到 B 的方法/理论/工具。`
- 简洁、白话，避免百科式定义

#### 本质（Essence）
> 它真正解决的底层机制是什么？
- 用类比帮助理解
- 解释为什么有效，不重复定义

#### 来源与依据（Source & Evidence）
> 为什么值得相信？
- 提出者、理论来源、经典实验、研究发现
- 例如：生成效应（Slamecka & Graf, 1978）；主动回忆（Karpicke & Roediger, 2008）
- **如果概念没有明确科学研究支持，标注：**「（暂无明确研究依据，属于经验模型）」

#### 常见误解（Misconception）
> 它不是什么？
- 区分相似概念，破除错误理解

---

### 3. 关系（Relation）
- 先用 `search_files` 扫描 vault：`search_files(pattern="核心词", path="~/projects/wsj-second-brain/03-Resources/Evergreen", target="content")`
- 用 `[[卡片名]]` 建立双向链接（Obsidian 双链格式）
- 按「上位→相似→对立→应用」分类，每类 1-3 个
- 找不到的维度写「—」，不硬凑

---

### 4. 案例（Example）
**优先级（严格遵守）：**
1. **用户明确提供**的真实经历
2. **vault 中已有记录**的线索
3. **公开真实案例**（历史事件、名人经历、实验研究）
4. **简单类比**

> ⚠️ **禁止虚构用户经历。** 不要写「你之前在跑步时发现…」「你曾经在编程中…」。
> 没有前两级素材时，直接用公开案例或类比。

---

### 5. 行动（Action）
- **不给建议，给步骤**：能立刻执行的 3-5 步
- 用动词开头：`打开 Obsidian → 新建笔记 → ……`
- 涉及 Obsidian 操作时，指定具体路径/模板

---

### 6. 边界（Boundary）
> 公共知识的边界——什么时候有效，什么时候不适用。
- 适用场景 / 不适用场景 / 常见误区
- 用 `| 适用 | 不适用 |` 对比表

---

### 7. 内化（Insight）
> ⚠️ **此节不由 AI 生成。AI 输出留空模板，用户自行填写。**

AI 输出（表格格式，与整卡风格一致）：

```markdown
## 7. 内化（Insight）

> [!abstract] ⚠️ 此部分由你填写
> AI 提炼公共知识，你沉淀私人知识。

| 维度 | 你的回答 |
|------|---------|
| **我的理解** | （这个知识让你重新理解了什么问题？） |
| **我的经历** | （你的哪段经历验证或挑战了它？） |
| **我的迁移** | （还能用在哪些领域？怎么用？） |
```

旧版缩进列表格式已废弃，不再使用：

```markdown
## 7. 内化（Insight）
> ⚠️ 此部分需要你补充。
### 我的理解
-
### 我的经历
-
### 我的迁移
-
```

**为什么 Insight 放最后？**

```
Problem → Concept → Relation → Example → Action → Boundary
↑—— 公共知识，AI 可以生成 ——↑

Insight
↑—— 私人知识，只有你能填 ——↑
```

> 世界知识 → 我的认知。让个人内容永远收束在最后。

---

## 执行流程

```
收到请求
  ↓
Step 1: 理解概念 → 从痛点出发
  ↓
Step 2: AI 生成 6 节（Problem/Concept/Relation/Example/Action/Boundary）
  Insight 输出留空模板，不编造认知。
  ↓
⚠️ Step 3: 展示给用户
  - 直接展示卡片内容，不做解释
  - 等待反馈（"可以"/"改一下XX"/"加一个XX案例"）
  - 用户确认后才继续 ↓
  ↓
Step 4: 自查（见质量检查清单）
  ↓
Step 5: 检查冲突
  search_files(pattern="核心词", path="03-Resources/Evergreen", target="files")
  - 不存在 → 保存
  - 已存在 → 问：更新还是新建？
  ↓
Step 6: 写入文件
  path: ~/projects/wsj-second-brain/03-Resources/Evergreen/{核心词}.md
```

---

## 批量重写已有卡片

当用户要求**将一批已有卡片升级到新格式**时（如补充视觉化表格、重新排版、调整 callout 类型），使用以下流程：

### 流程

```
收到请求（如"重写 8 张卡片为新格式"）
  ↓
Step 1: 搜索所有源卡片文件
  search_files(path="~/projects/wsj-second-brain/03-Resources/Evergreen",
               pattern="{卡片名}")
  ↓
Step 2: 逐张读取源卡片
  read_file(path="{卡路径}")
  ↓
Step 3: 对每张卡片，将旧内容映射到新格式 7 节
  保留：核心概念定义、来源依据、个人体验、行动指引、参考资料
  重构：将旧条目归类到新模板的对应节
  新增：Problem 警示表、来源表格、误解表格、关系表格、边界表、留空 Insight
  ↓
Step 4: 输出全部卡片到同一文件（或逐文件覆盖）
  - 用 `### 卡片：名称` 作为卡片分隔
  - **卡片之间不用 `---` 分割线**
  - 每张卡片独立 frontmatter（简化版：无 source_type/confidence/created/modified）
  ↓
Step 5: 验证
  - `grep -c "^### 卡片："` = 卡片总数
  - `grep -c "^---$"` = 卡片数 × 2（仅 frontmatter 分隔符，无多余分割线）
  - `grep -c "⚠️ 此部分由你填写"` = 卡片总数（每卡均有留空 Insight）
```

### 格式映射

| 旧卡片内容 | 新格式目标节 | 说明 |
|------------|------------|------|
| 核心概念下的要点 | Concept（📖 定义 / 🔍 本质） | 单点展开为段落 |
| 个人体验 | Example（`[!example]`） | 转化为第三人称案例，不自称"你" |
| 行动指引 | Action（`[!check]` + 步骤表） | 转化为步骤表格 |
| 参考资料 | Concept（🔬 来源与依据） | 转化为来源表格 |
| —（新增） | Problem（`[!warning]` + 以为/实际表） | 从核心概念推导常见误解 |
| —（新增） | Relation（类型表） | 扫描已有 vault 建立 [[wikilinks]] |
| —（新增） | Boundary（✅/❌ 表） | 判断适用/不适用场景 |
| —（留空） | Insight（`[!abstract]`） | 保留模板，不填充 |

### 常见陷阱

- ❌ 在卡片之间加 `---` 视觉分割线 → 使用 `### 卡片：` 标题自然分隔
- ❌ 在 Insight 节填充内容 → 必须留为空白模板
- ❌ **批量重写全部 Insight 被 AI 污染** — 2026-07-10 发现 50 张卡 Insight 全部被填了 AI 生成的问题（「我在哪个场景…」「我最近一次…」）。根本原因：子 agent 批量重写时 Insight 留空规则未被强制执行。修复：全量清洗为统一空模板。**批量重写后必须逐张验证 Insight 尾部无 AI 内容。**
  - 验证命令（在 `03-Resources/Evergreen/` 目录执行）：
    ```bash
    for f in *.md; do
      after=$(sed -n '/^## 7. 内化/,$p' "$f" | tail -n +2)
      real=$(echo "$after" | grep -v '⚠️\|你填写\|AI 提炼\|维度\|理解\|经历\|迁移\|^|-')
      [ -n "$(echo "$real" | grep -v '^$')" ] && echo "⚠️ $f"
    done
    ```
    输出为空 = 全部干净。有输出 = 对应卡片还有 AI 内容，逐个清洗。
- ❌ **不要删除原始任务项** — 马拉松/旅居/王轻舟等日志笔记中的 `- [x]` 和 `- [ ]` 任务项是核心数据，只能重组格式不能删除。笔记整理模式：顶部仪表盘总览 + 原始数据用 `[!info]-` 折叠保留。
- ❌ 保留旧 frontmatter 的 `created`/`modified`/`source_type`/`confidence` → 仅保留简化版字段
- ❌ 忽略 vault 中已有的 [[双链]] 关系 → 必须用 `search_files` 扫描 existing vault
- ❌ 多张卡片输出时混在一起 → 每张卡片起始行必须是 `### 卡片：{名称}`
- ❌ **编号级联错误** — 写完 Concept（节 2）后可能误标 Relation 为「4」而非「3」，造成后续连续错位。写完后 grep 验证：`grep "^### [1-7]\."` 应正好 7 行，按序 1→2→3→4→5→6→7。
- ❌ **遗漏「7. 内化（Insight）」标题** — `> [!abstract]` 上方容易忘记加 `### 7. 内化（Insight）` 标题头。卡片可能通过了「Insight 留空检查」（因为有 `⚠️`），但实际缺少标题头。额外验证：`grep "### 7\. 内化"` 应为卡片总数。
- ❌ **旧 format 残留** — 旧卡片可能使用 `- 核心概念` / `- 个人体验` / `- 行动指引` / `- 参考资料` 无序列表结构，以及 frontmatter 中 `description: dv.view(...)`、旧 tags（如 `[百度工作经验, 百度算法工程师]`）。迁移后需清理：description 替换为真实句子、tags 统一为 `[常青卡片]`。
- ❌ **区块标题层级错误** — Subagent 可能误用 H3 而非 H2，导致整张卡缺少主节标题。这是最常见的批处理翻车场景。
- ❌ **重复节标题** — 后处理脚本（如 sed/python regex）可能误插重复的节标题，如 `## 6. 边界` 出现两次。验证：`grep -c "^## 6\\."` = 1，对所有 1-7 节逐一确认。

## 边界条件处理

### 素材不足
- 只给关键词 → 用自己的知识填 6 节；不确定的标注 `（待查）`
- 关键词太模糊（「那个学习的方法」）→ 追问：「具体是哪个？」不猜

### 从剪藏素材生成
- 先 `search_files` 在 `00-LLM-WiKi/Raw/` 定位文章
- 用自己的话重组，不整段复制

### 知识概念模糊
- 用 `web_search` 查定义和核心观点
- **洞察不搜**——搜来的只能填概念/关系/案例

### 概念缺乏科学依据
- 在「来源与依据」处如实标注：`（暂无明确研究依据，属于经验模型）`

### 与已有卡片冲突
- 保存前 `search_files(pattern="文件名", path="03-Resources/Evergreen")`
- 已存在 → 问用户：更新还是新建？

---

## 质量检查清单

- [ ] **问题**：一读就知道「什么时候用」？
- [ ] **概念**：一句话定义 + 来源依据 + 常见误解 四项完整？
- [ ] **概念**：如果缺乏科学研究，已标注 `（暂无明确研究依据）`？
- [ ] **关系**：至少 2 条 `[[wikilink]]` ？
- [ ] **案例**：用了真实案例？**没有编造用户经历**？
- [ ] **行动**：步骤具体到能立刻执行？
- [ ] **边界**：适用/不适用/常见误区 三项都有？
- [ ] **内化**：确认已输出为留空模板，**没有 AI 代写**？
- [ ] **Frontmatter**：source、status 都填了？（batch rewrite 时检查无 source_type/confidence/created/modified 遗留）
- [ ] **Batch rewrite 额外检查**：卡片间无 `---` 分割线？`^---$` 计数 = 卡片数 × 2？Insight 留空数 = 卡片数？
- [ ] **编号序列检查**（batch rewrite & single card）：`grep "^### [1-7]\\."` 正好 7 行且顺序为 1→2→3→4→5→6→7 无跳跃？
- [ ] **标题 7 存在检查**：`grep "### 7\\. 内化"` 计数 = 卡片数？（仅 `> [!abstract]` 不够，必须有标题头）

---

## 成熟度状态说明

| status | 含义 | 何时升级 |
|--------|------|---------|
| `seed` | 刚接触这个概念 | 初始状态 |
| `learning` | 系统学习过，能解释 | 读完相关内容后 |
| `practiced` | 在真实场景中用过 | 实践后回来更新 |
| `evergreen` | 已验证可复用，形成体系 | 跨领域多次应用后 |

新卡默认 `learning`。实践后手动改为 `practiced`。

---

## 可信度等级

| level | 含义 | 示例 |
|-------|------|------|
| `high` | 有实验研究或大量数据支持 | 生成效应、遗忘曲线 |
| `medium` | 方法有效，但非正式科学理论 | 费曼学习法、GTD |
| `low` | 个人经验或观点，尚未广泛验证 | 某人总结的学习技巧 |

Source type + confidence 组合让 AI 未来能够区分：科学研究 / 经验总结 / 作者观点。

---

## 文件命名规则

- 文件名 = 核心词（中文），如 `费曼学习法.md`
- 不要 `AI_` 前缀，不要编号
- 路径：`~/projects/wsj-second-brain/03-Resources/Evergreen/`

---

## Obsidian Callout 格式化（推荐）

用 Obsidian callout 区块让卡片更有呼吸感和视觉层次。**节间不用 `---` 分割线，靠标题和 callout 自然分隔。**

| Callout | 用途 | 示例 |
|---------|------|------|
| `> [!warning]` | **Problem 节** — 开门警示「你以为 vs 实际」信念冲突表 | `> [!warning] 你以为学得快就是学得好，结果发现忘得也快` |
| `> [!info]` | 信息框 — 来源依据说明、补充研究背景 | `> [!info]- 关于研究依据`（`-` = 折叠） |
| `> [!example]` | **Example 节** — 真实案例 | `> [!example] 你可能经历过`，内部可嵌套 `[!tip]` |
| `> [!check]` | **Action 节** — 步骤清单 | `> [!check] 建立复习系统` + 步骤表 |
| `> [!tip]` | 提示框 — 嵌套在 Example 中的小型洞察 | `> [!tip] 西蒙的"烧水比喻"` |
| `> [!abstract]` | **Insight 节** — 留空占位 | `> [!abstract] ⚠️ 此部分由你填写` + 三行模板表 |
| `> [!quote]` | 引用框 — 核心总结、金句 | `> [!quote] 📌` |

**不需要的：**
- ❌ **节间 `---` 分割线** — callout + 标题已足够分隔（卡片之间也不用）
- ❌ 过长的 frontmatter — 简化版够用（batch rewrite 时尤其不要保留 created/modified/source_type/confidence）
- ❌ 居中 `<div>` 包装正文内容 — **用户已明确拒绝**，不要在任何卡片中加 `<div align="center">` 标题区
- ❌ Insight 节填充 AI 内容 — 必须留空模板

---

## Knowledge Pipeline

```
信息输入
  书籍 · 文章 · 视频 · 聊天 · 想法
    ↓
┌─────────────────────────────────┐
│ clip                             │
│ 收集原始素材                      │
│ → Raw                            │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ knowledge-card                   │
│ 知识蒸馏                          │
│                                  │
│ Problem                          │
│ Concept（定义/本质/来源/误解）      │
│ Relation（[[wikilinks]]）         │
│ Example                          │
│ Action                           │
│ Boundary                         │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ Human                            │
│ 个人内化                          │
│                                  │
│ Insight                          │
│ （我的理解/经历/迁移）             │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ Evergreen                        │
│ 长期知识资产                      │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ AI Retrieval                     │
│ 未来 AI 基于个人知识库回答         │
└─────────────────────────────────┘
```

---

## 一句话

> **把互联网知识，通过 AI 蒸馏成结构化知识，再通过人的实践和反思，转化成个人资产。**

---

> [!warning] **不附带不完整的示例卡片**
>
> 用户曾明确拒绝了一份未完善的 `references/example-费曼学习法.md`。规则：
> - 示例/参考卡片必须内容完整、格式正确，**不得有"待补""预留"等占位**
> - 如示例未就绪，宁可先不放，等完善后再追加
> - 删除示例时同步清理 SKILL.md 中的引用链接

---

## 附：设计决策

本 skill 的架构决策记录见 `references/design-decisions.md`（Insight 分离、Concept 升级、Boundary 位置、Pipeline 等设计原因）。
